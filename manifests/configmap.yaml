apiVersion: v1
kind: ConfigMap
metadata:
  name: voting-app-config
data:
  POSTGRES_HOST: "__POSTGRES_ENDPOINT__"
  POSTGRES_PORT: "__POSTGRES_PORT__"
  POSTGRES_DB: "__POSTGRES_DB__"
  REDIS_HOST: "__REDIS_ENDPOINT__"
  REDIS_PORT: "__REDIS_PORT__"
